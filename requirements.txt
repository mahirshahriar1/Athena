# Backend dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==12.0

# LangChain + LangGraph
langchain>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.28

# LLM Providers (both supported — pick via LLM_PROVIDER env var)
langchain-groq>=0.2.0            # FREE: Groq cloud (llama3, mixtral)
langchain-anthropic>=0.2.0       # PAID: Claude Sonnet

# Embedding Providers (both supported — pick via EMBEDDING_PROVIDER env var)
langchain-huggingface>=0.1.0     # FREE: local sentence-transformers
sentence-transformers>=3.0.0     # Required for huggingface embeddings
langchain-openai>=0.2.0          # PAID: OpenAI text-embedding-3-small

# Scraping
beautifulsoup4==4.12.3
ddgs>=9.0.0
lxml==5.3.0

# RAG - ChromaDB local
chromadb>=0.5.0

# Utils
pydantic>=2.8.0
python-dotenv==1.0.1

# PDF report generation
jinja2==3.1.4
markdown==3.7
